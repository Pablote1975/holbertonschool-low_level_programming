#include "main.h"
/**
*print_sign - Moneda 3 de C
* @n: Variable desconocida
*Return: 0
*/

int print_sign(int n);

if (n > 0)
  return ('+');
 else (n = 0)
	return ('0');
 else (n < 0)
	return ('-');
