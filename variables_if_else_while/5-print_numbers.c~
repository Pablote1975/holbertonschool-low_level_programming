#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
*main- last digit %
*Moneda 3 task 5
*Return:0
*/

int main(void)
{
int decimos;
for (decimos = 0; decimos <= 10; decimos++)
{
printf("%d", decimos);
}
printf(\n);
return (0);
}
