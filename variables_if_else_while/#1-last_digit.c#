#include <stdlib.h>
#include <time.h>
/**more headers goes there
*Moneda 1 task 1
* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
	return (0);
}