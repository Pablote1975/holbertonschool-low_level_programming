#include "main.h"

/**
 *_strcat- M14
 *@dest:varia
 *@src:varia
 *@n:varia
 *Return:
 */
char *_strncpy(char *dest, char *src, int n)
{
  int l = 0;
  for (; l < n && src[l] != '\0'; l++)
    {
      *dest[l] = *src[l];
    }
