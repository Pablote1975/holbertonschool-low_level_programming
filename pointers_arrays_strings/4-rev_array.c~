#include "main.h"

/**
 *reverse_array- M14
 *@a:varia
 *@n:varia
 *Return:
 */
void reverse_array(int *a, int n)
{
  int b, i, j;
  for (i = 0; i < j; i++)
    for (j = 0; i < j; j--)
      {
	b = a[i];
	a[i] = a[j];
	a[j] = b;
      }
}
