#include "main.h"

/**
 *reset_to_98- Moneda 13
 *@n:variable
 *Return:98
 */

void print_rev(char *s)
{
int len = 0;
while (*s != '\0') {
len++;
s++;
}
s--;

while (len--) {
putchar(*s);
s--;
}
putchar('\n');
}
