#include "main.h"
#include <stdio.h>
/**
* main - prints its number and new line
 * @argc: lo que piden
 * @argv: nombre del comando
 * Return: numbers
 */
int main(int argc, char *argv[])
{
int i;
for (i = 0; i < argc; i++)
{
_putchar("%s\n", argv[i]);
}
return (0);
}
