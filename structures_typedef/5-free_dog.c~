#include "dog.h"
#include <stdlib.h>
/**
 **new_dog- Moneda 20
 *@name: name
 *@age: age
 *@owner: owner
 * variable
 *return:dog
 */

void free_dog(dog_t *d)
{
if (d != NULL)
{
free(d->name);
free(d->owner);
free(d);
}
}
